{
  "homepage": "https://NqobileRSA.github.io/portfolioV1",
  "name": "portfolioV1",
  "version": "1.0.0",
  "description": "A simple HTML, CSS, JS website",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "build": "mkdir -p dist && cp *.html dist && cp *.css dist && cp *.js dist"
  },
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
